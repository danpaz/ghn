import diffHighlight from 'gh-notification-flow/utils/diff-highlight';
import { module, test } from 'qunit';

module('Unit | Utility | diff highlight');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = diffHighlight();
  assert.ok(result);
});
